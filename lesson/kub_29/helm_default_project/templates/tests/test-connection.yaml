apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm_default_project.fullname" . }}-test-connection"
  labels:
    {{- include "helm_default_project.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm_default_project.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
